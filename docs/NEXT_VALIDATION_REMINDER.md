# Latest Validation Note

The GearBlocks runtime API interface availability change has been validated in-game on 2026-06-15.

Validated result:

- A refreshed exporter ran against Rob's vehicle.
- Randomly checked functional parts showed API attributes without values in the Parts catalog.
- The DB definition / runtime construction export context is expanded with indexed interface availability metadata.
- API metadata is not part of default game-chat prompt context unless a future explicit include control is added.
