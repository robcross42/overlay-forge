# Overlay Forge Codex Quick Reference

Read `AGENTS.md`, then the smallest relevant host document. This repository owns only the typed private launcher. Product features and settings belong in their Media, Gaming, or Retirement repositories.

Validate frontend changes with `npm.cmd run build`, backend changes with `npm.cmd run cargo:build`, and broad changes with `npm.cmd run cargo:clippy` plus `git diff --check`.
